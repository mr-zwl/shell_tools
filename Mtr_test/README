# MTR Route探测脚本

此脚本用于在多台主机之间进行MTR路由探测。最好在具有所有主机免密登录权限的控制机上执行。

## 配置与执行

1. 修改 `run.sh` 中的 `IP_LIST` 变量，指定需要进行探测的IP地址。
2. 确保 `mtr_temp_file.sh` 具有执行权限（如果需要）：`chmod +x mtr_temp_file.sh`
3. 修改IP地址后，执行：
bash run.sh
4. 执行完成后会生成日志
cat all_mtr_results.log
